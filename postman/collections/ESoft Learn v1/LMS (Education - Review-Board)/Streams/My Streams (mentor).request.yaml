$kind: "http-request"
url: "{{baseUrl}}/api/lms/streams/my/mentor"
method: "GET"
documentation:
  content: "Мои потоки как ментора. profileId берётся из JWT (req.currentUser). Только роль MENTOR (ADMIN проходит авторизацию, но получит 403 — нет профиля)."
examples: "./.resources/My Streams (mentor).resources/examples"
order: 8600
